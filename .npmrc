# Force npm to run in production mode
# This ensures consistent builds
production=false

# Don't generate a package-lock file
package-lock=false

# Don't fail on peer dependency issues
legacy-peer-deps=true

# Cache settings for faster installations
prefer-offline=true
